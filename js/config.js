// Filled in once the Supabase project exists (Project Settings -> API).
// SUPABASE_ANON_KEY is safe to expose client-side -- it only grants what
// Row Level Security policies allow (public read on routes/airports/codeshare_partners).
// Never put the service_role key here.
export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
